/* return house or objNull */

#include "..\..\component.hpp"

params ["_allPlayers", "_minSpawnDistance", "_maxSpawnDistance"];

if (count _allPlayers == 0) exitWith {INFO("_allPlayers is empty"); [0,0,0]};

private _houses = [selectRandom _allPlayers, _minSpawnDistance, _maxSpawnDistance, 10, "house"] call grad_civs_fnc_findSpawnPositionCandidates;

if (count _houses > 0) then {
    selectRandom _house;
} else {
    objNull;
}
